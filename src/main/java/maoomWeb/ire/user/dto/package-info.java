/**
 * 화면, API, 서비스, mapper 사이에서 데이터를 운반하는 DTO 모음.
 *
 * <p>DTO는 로직을 거의 넣지 않고 필드 의미를 분명하게 유지하는 역할을 한다.
 * DB 컬럼과 직접 연결되는 DTO는 mapper XML의 resultMap 또는 SQL alias와 필드명이
 * 맞아야 한다.</p>
 */
package maoomWeb.ire.user.dto;
