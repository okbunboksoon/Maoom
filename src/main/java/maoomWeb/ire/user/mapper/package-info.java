/**
 * MyBatis XML mapper와 연결되는 DB 접근 인터페이스 모음.
 *
 * <p>인터페이스 메서드 이름은 {@code src/main/resources/mapper} 아래 XML의
 * {@code select/update/insert/delete id}와 맞물린다. SQL을 수정할 때는 Java
 * mapper와 XML mapper를 함께 확인해야 한다.</p>
 */
package maoomWeb.ire.user.mapper;
